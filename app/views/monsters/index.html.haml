.index-wrapper
  .your-monster
    あなたの所持モンスター
  .monsters-index
    - @monsters.each do |monster|
      .monster-index
        = link_to monster_path(monster) do
          = monster.name
          %img{src:monster.image}